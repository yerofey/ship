{
  "name": "the-ship",
  "module": "src/index.js",
  "type": "module",
  "scripts": {
    "docker:build": "docker build -t the-ship .",
    "docker:run": "docker run -d -p 3099:3099 the-ship",
    "start": "pm2 start index.js --name the-ship --time --i 1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "nodemon": "^3.0.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@fastify/cors": "^8.4.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/rate-limit": "^9.0.1",
    "dotenv": "^16.3.1",
    "fastify": "^4.24.3"
  }
}
